name: learn-github-actions
run-name: ${{ github.actor }} triggered an action
on:
  pull_request:
    branches:
      - main
      - staging
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged